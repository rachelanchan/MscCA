/*
Date: 12 June, 2024
Author: Rachel Anchan

WAP for using recursion to calculate factorial of a number.
*/

#include<iostream.h>
#include<conio.h>
int fact(int num)
{
	if(num==0)
	{
		return 1;
	}
	
	else{
		return num*fact(num-1);
	}
};

void main()
{       
	clrscr();
	
	int num;
	cout<<"Enter a number: "<<endl;
	cin>>num;
	
	fact(num);
	
	getch();
}